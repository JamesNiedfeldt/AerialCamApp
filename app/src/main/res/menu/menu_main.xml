<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/wifi_settings"
        android:title="@string/wifi_settings"
        app:showAsAction="never" />

    <item
        android:id="@+id/length_settings"
        android:title="@string/length_settings"
        app:showAsAction="never" />

    <item
        android:id="@+id/framesize_settings"
        android:title="@string/framesize_settings"
        app:showAsAction="never" />

    <item
        android:id="@+id/quality_settings"
        android:title="@string/quality_settings"
        app:showAsAction="never" />

</menu>